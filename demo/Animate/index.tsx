import React, { FC } from "react";

import { container, title } from "../theme";

const Animate: FC = () => {
  // ...

  return (
    <div css={container}>
      <h1 css={title}>This is title</h1>
    </div>
  );
};

export default Animate;
